spring.application.name=call_backend

# ========================================
# PostgreSQL Database Configuration
# ========================================
# Using credentials provided by user (Render Postgres)
spring.datasource.url=jdbc:postgresql://dpg-d4nivq7pm1nc73e8d7h0-a.oregon-postgres.render.com:5432/call_db_eg84
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=call_db
spring.datasource.password=LbyiNPqztfJwq6qtiDJ9zSBL7osQpBKN

# ========================================
# JPA/Hibernate Configuration
# ========================================
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Disable H2 console when using Postgres
spring.h2.console.enabled=false

# ========================================
# Server Configuration
# ========================================
server.port=8080
server.address=0.0.0.0

# ========================================
# JWT Configuration
# ========================================
jwt.secret=my-super-secret-key-for-jwt-token-generation-with-at-least-256-bits-for-security
jwt.expiration=86400000

# ========================================
# Actuator Configuration (Health Check)
# ========================================
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized
management.health.defaults.enabled=true
