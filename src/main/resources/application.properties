spring.application.name=call_backend

# ========================================
# PostgreSQL Database Configuration
# ========================================
# Using environment variables for security (set via Fly.io secrets)
# URL format: jdbc:postgresql://host:port/database
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://dpg-d4nivq7pm1nc73e8d7h0-a.oregon-postgres.render.com:5432/call_db_eg84}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:call_db}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:LbyiNPqztfJwq6qtiDJ9zSBL7osQpBKN}

# ========================================
# JPA/Hibernate Configuration
# ========================================
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# Disable H2 console when using Postgres
spring.h2.console.enabled=false

# ========================================
# Server Configuration
# ========================================
server.port=8080
server.address=0.0.0.0

# ========================================
# JWT Configuration
# ========================================
jwt.secret=my-super-secret-key-for-jwt-token-generation-with-at-least-256-bits-for-security
jwt.expiration=86400000

# ========================================
# Actuator Configuration (Health Check)
# ========================================
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized
management.health.defaults.enabled=true
